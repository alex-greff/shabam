import { Request, Response, NextFunction } from "express";

export const search_address_db = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {
  // Send response
  res.status(200).json({ message: "TODO: implement" });
};

export const add_track_addresses = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {
  // Send response
  res.status(200).json({ message: "TODO: implement" });
};

export const delete_track_addresses = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {
  // Send response
  res.status(200).json({ message: "TODO: implement" });
};

export const get_address_database = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {
  // Send response
  res.status(200).json({ message: "TODO: implement" });
};
