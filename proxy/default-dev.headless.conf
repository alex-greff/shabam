# Nginx setup for headless development (i.e. no webapp client)

upstream api {
  server api:5000;
}

server {
  listen 80;

  location /api {
    rewrite /api/(.*) /$1 break; # Cut off the /api part and just pass the remainder to the server
    proxy_pass http://api;
  }
}