import React, { FunctionComponent } from "react";
import "./Catalog.scss";

const Catalog: FunctionComponent<{}> = () => {
    return (
        <div id="Catalog">
            Catalog View
        </div>
    );
};

export default Catalog;