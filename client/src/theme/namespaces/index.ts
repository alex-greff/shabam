import { NamespaceData } from "@/themer-react";
import { DEFAULT_NAMESPACE, DEFAULT_THEME } from "@/constants";

const defaultNamespace: NamespaceData = {
  namespaceId: DEFAULT_NAMESPACE,
  targetTheme: DEFAULT_THEME
};

export default [defaultNamespace];
