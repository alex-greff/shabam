schema: ${SCHEMA_URL}
documents: ./src/**/*.graphql
generates:
  ./src/graphql-apollo.g.d.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
  ./src/graphql-request.g.d.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
