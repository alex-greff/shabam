mutation AddTrack($trackData: TrackAddDataInputWithFile!) {
  addTrackWithFile(trackData: $trackData) {
    id
    addressDatabase
    metadata {
      title
      artists {
        name
        type
      }
      coverImage
      duration
      numPlays
      releaseDate
      createdDate
      updatedDate
    }
  }
}