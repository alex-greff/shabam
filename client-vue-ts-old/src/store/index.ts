import Vue from 'vue';
import Vuex from 'vuex';

import ThemeStore from "./theme";

Vue.use(Vuex);

export default new Vuex.Store({
    state: {},
    mutations: {},
    actions: {},
    modules: {
        ThemeStore
    }
});
